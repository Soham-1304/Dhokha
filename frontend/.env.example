# Leave unset during local development to use the Vite /api proxy.
# Set this to the public AWS backend URL for a deployed frontend.
VITE_API_URL=
VITE_WS_URL=wss://api.dhokha.bharathperni.dev/stream
